function mostrarInfo(cadena)
{
    for (var index = 0; index < cadena.length; index++) 
    {
        cadena.charAt(index);
            
    }
}
window.onload = function() 
{
    
    mostrarInfo("HOLA");
}
