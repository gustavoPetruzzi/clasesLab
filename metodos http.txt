GET-POST--> pedir cosas al servidor

AJAX, JQUERY, JSON


metodo javascript para request

request type
0--> no se envio
1--> se llama al open
2--> llega al servidor
3---> cargando la respuesta--> devuelve el servidor
4---> ya esta. Done


		
var req = new XMLHttpRequest();
req.open('GET', 'url del recurso de lo que estoy buscando'(localhost//get/algo) , true) // true--> indica que es async


callback---> agarra lo que devuelve send(boton)---> fue por otro hilo

req.onReadyStateChange = function recibir() {
	if(request.readystate == 4)
	// ya tengo la respuesta del servidor
	{
		req.responseText;
		miDivHtml.innerHtml = req.responseText;	
	}
	else
	{
		miDivHTML.innerHtml = "CARGANDO";
	}
}

req.send()

